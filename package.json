{
  "name": "enhanced-dispo-fetch",
  "version": "1.0.0",
  "description": "",
  "main": "test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && rsync -av --exclude='*.ts' browser-extension/ dist/",
    "clean": "rm -rf dist",
    "lint": "web-ext lint"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/firefox-webext-browser": "^120.0.3",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
